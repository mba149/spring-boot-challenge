Make sure You have these set
% export AWS_ACCESS_KEY_ID="anaccesskey"
% export AWS_SECRET_ACCESS_KEY="asecretkey"
% export AWS_REGION="us-west-2"

This launches a DynamoDB for state locking and S3 bucket for remote state file.
the /envs folder contains the tfvars files as per environment.